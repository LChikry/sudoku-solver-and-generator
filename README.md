# Sudoku Solver & Generation

the `sudoku` package has all the necessary files to generate and solve Sudoku puzzles. You can have it in your codebase and instantiate the `Sudoku` class to work with it. Also, you run this program and you will get to interact with package via the terminal and test its features/methods.
